════════════════════════════════════════════════════════════
 PClinic hub — macOS 27 "Golden Gate" FEATURE edition (2026-09-20)
 Do NOT upload this .txt — instructions only.
════════════════════════════════════════════════════════════
hub.html keeps YOUR hub exactly as it was — same header line, same
stat cards, same department cards with descriptions/badges/arrows,
same roles, search, stats and auth logic. On top of it, this file
adds the macOS 27 Golden Gate feature set:

  • Liquid Glass material — every existing glass surface (cards,
    header, search) upgraded via PClinic's own CSS tokens; density
    is live-adjustable: Control-Centre button (sliders icon, header
    right) → "Liquid Glass" slider, Ultra Clear ↔ Tinted (saved).
  • Siri AI assistant — rainbow orb button in the header line opens
    a glass chat window. Try: "open lab", "how many in queue",
    "how many patients today", "dark mode", "make glass clearer",
    "meeting tomorrow at 9", "who am i", "help".
  • Spotlight "Search or Ask" — search icon in the header (or
    Ctrl/Cmd+F / Ctrl/Cmd+K): module results + questions are handed
    to PClinic AI as an answer row; Enter/click executes. It stays
    in sync with the hub's own module search and role filter.
  • Visual Intelligence — Ctrl/Cmd+Shift+V (or the focus icon):
    click any card/stat → contextual explanation + Open / Ask buttons.
  • Context-aware quick actions — right-click any card or stat:
    Open, Open in New Tab, Ask Assistant, Visual Intelligence.
  • Pull to refresh — pull/scroll up hard at the top of the page:
    live stats refresh with a glass spinner.
  • Natural-language reminders — "meeting tomorrow at 9" via the
    assistant creates an "Up next" chip under the header (per
    browser, removable with ×).

Upload: https://github.com/nkundimanajeanpaul1-hash/Pclinic/upload/main
  • drag hub.html to the repository ROOT → "Replace existing file?" → yes
  • commit message suggestion: "Hub: add macOS 27 Golden Gate features"
  • nothing else to change; no ?v= cache-busting (HTML is no-store;
    the feature layer is inline CSS/JS; icons use the existing Tabler font)
